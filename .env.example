# Local development — JSON store works without secrets.
# Optional: enable real AI summaries (otherwise a local heuristic is used).
# OPENAI_API_KEY=
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_MODEL=gpt-4o-mini
